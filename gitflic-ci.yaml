image: node:20

stages:
  - install
  - build
  - test

jobs:
  install:
    stage: install
    scripts:
      - npm install

  build:
    stage: build
    scripts:
      - npx tsc --noEmit

  test:
    stage: test
    scripts:
      - npm run test:run