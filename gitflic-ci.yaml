image: node:20

stages:
  - install
  - build
  - test

install_deps:
  stage: install
  script:
    - npm install

build_app:
  stage: build
  script:
    - npx tsc --noEmit

test_app:
  stage: test
  script:
    - npm run test:run