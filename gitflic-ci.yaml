image: node:20

stages:
  - install
  - build
  - test

jobs:
  - name: install
    stage: install
    scripts:
      - npm install

  - name: build
    stage: build
    scripts:
      - npx tsc --noEmit

  - name: test
    stage: test
    scripts:
      - npm run test:run